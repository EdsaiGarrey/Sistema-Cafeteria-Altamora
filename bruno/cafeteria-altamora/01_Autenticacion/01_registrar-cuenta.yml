info:
  name: 01_registrar-cuenta
  type: http
  seq: 1

http:
  method: POST
  url: http://82.25.93.110:8082/api/autenticacion/registro
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "uriel eduardo",
        "email": "urieledu@gmail.com",
        "password": "{{usuario_password}}",
        "password_confirmation": "{{usuario_password}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

