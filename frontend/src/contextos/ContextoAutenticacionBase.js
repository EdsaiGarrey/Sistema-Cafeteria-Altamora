import { createContext } from 'react'

/*
 * Contexto que permite compartir la información de la sesión
 * entre los componentes de la aplicación.
 */
export const ContextoAutenticacion = createContext(null)